# 기능 명세서: Gemma Glance

본 문서는 Gemma Glance의 핵심 기능과 동작 방식을 정의합니다.

---

## 1. 핵심 기능 (Core Features)

### 1.1 실시간 문맥 분석 모드 (Real-time Analysis Mode)
- **기능 정의**: 활성화 상태일 때 마우스 포인터 주변의 텍스트를 자동으로 감지하여 분석 결과를 제공합니다.
- **동작 방식**:
    - 마우스가 특정 위치에 일정 시간(예: 0.5초) 머무를 경우 텍스트 추출 트리거 발동.
    - 추출된 텍스트와 주변 문맥을 Gemma 4 E2B 모델로 전달.
    - 분석된 설명을 마우스 옆 오버레이 창에 표시.

### 1.2 분석 모드 토글 (Analysis Toggle)
- **기능 정의**: 전역 단축키를 통해 분석 기능을 즉시 켜거나 끌 수 있습니다.
- **단축키**: `Alt + Q` (기본값, 설정 가능)
- **상태 피드백**:
    - 시스템 트레이 아이콘 색상 변경 (On: 녹색/파랑, Off: 회색).
    - 모드 전환 시 화면 하단에 가벼운 알림(Toast) 표시.

### 1.3 시스템 트레이 메뉴 (System Tray Menu)
- **기능 정의**: 시스템 트레이 아이콘을 통해 앱의 상태를 확인하고 주요 설정을 제어합니다.
- **주요 항목**:
    - **분석 모드 활성화/비활성화**: 체크박스 형태로 현재 상태 표시 및 수동 전환.
    - **설정 (Settings)**: 단축키 변경, 감지 대기 시간(Hover time) 조절 등.
    - **모델 정보**: Gemma 4 E2B 엔진 상태 확인.
    - **종료 (Exit)**: 애플리케이션 완전 종료.

### 1.4 스마트 오버레이 (Smart Overlay)
- **디자인**: 투명도가 적용된 프레임리스 윈도우, 부드러운 그림자 및 라운드 처리.
- **표시 내용**: Gemma 4 E2B가 생성한 쉬운 용어 설명.
- **상호작용**: 
    - 마우스가 텍스트에서 멀어지면 자동으로 사라짐.
    - 모드 토글 OFF 시 현재 열려 있는 오버레이 즉시 닫힘.

---

## 2. 상세 동작 프로세스

1. **상태 대기**: 앱 실행 시 기본적으로 분석 모드는 `OFF` 상태.
2. **모드 활성화**: 사용자가 `Alt + Q` 입력 -> `ON` 상태로 전환.
3. **포인터 추적**: `ON` 상태에서 마우스 좌표 지속 감시.
4. **텍스트 추출**: Win32 API 또는 OCR을 사용하여 포인터 아래의 단어 및 주변 문장 획득.
5. **AI 추론**: Gemma 4 E2B가 문맥을 분석하여 "초등학생 수준의 한 문장 요약" 생성.
6. **결과 노출**: 텍스트 가독성을 고려한 위치에 오버레이 팝업.

---

## 3. 비기능적 요구사항

- **저사양 최적화**: 
    - 분석 모드가 `OFF`일 때는 CPU 사용량을 최소화(0%에 근접).
    - 추론 시에도 LiteRT를 활용해 시스템 지연 최소화.
- **개인정보 보호**: 모든 텍스트 추출 및 AI 연산은 로컬(Offline)에서만 수행.
- **안정성**: 백그라운드 프로세스로 동작하며 다른 프로그램의 포커스를 뺏지 않음.
